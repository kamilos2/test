package UnitTest;

import static org.junit.Assert.*;

import org.junit.Test;

public class tableTest { // pas de extends avec Junit4

	/*
	//@Before // @After
	public void initialiser() throws Exception {
	@Test(expected=IllegalArgumentException.class) --> veut cela
	@Test(timeout=100)
	}
	*/
	@Test // Annote chaque méthode de test
	public void testMain() {
		//assertEquals("est ce que nom est correct", "nom1", personne.getNom());
		//fail("Not yet implemented");
		//assertEquals(result, simpleCalculator.sub(a, b));
	}
	

}
